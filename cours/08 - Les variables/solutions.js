/*
 * Il existe une infinité de façons de résoudre un même problème.
 * Ces solutions ne sont que des exemples. Si vous avez utilisé un autre moyen
 * et que ça fonctionne, votre solution est valide aussi !
 */

// Chassez le bogue 0
let nomParc = "Le Royaume Magique";
console.log("Bienvenue au parc d'attractions " + nomParc);

// Chassez le bogue 1
let clientsHier = 359;
let clientsAujourdhui = 374;
let totalClients = clientsHier + clientsAujourdhui;
console.log(totalClients);

// Chassez le bogue 2
// "case" est un mot-clé réservé, vous n'avez pas le droit de l'utiliser comme nom de variable !
let nombreDeCases = 64;
let superficieCase = 2;
let superficie = nombreDeCases * superficieCase;
console.log("Superficie du plateau : " + superficie + "m2");

// Chassez le bogue 3
let heures = 13;
let minutes = 37;
let separateur = "h";
let horaire = heures + separateur + minutes;
console.log("Bienvenue, il est " + horaire);