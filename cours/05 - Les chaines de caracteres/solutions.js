/*
 * Tapez votre code sous chaque énoncé.
 * Rappel : pour afficher du texte il faut utiliser l'instruction console.log().
 */

// Affichez la phrase suivante : JavaScript est facile
console.log('JavaScript est facile'); // On peut utiliser les guillemets simples
console.log("JavaScript est facile"); // On peut aussi utiliser les guillemets doubles

// Affichez la phrase suivante : JavaScript c'est facile
console.log('JavaScript c\'est facile'); // On peut utiliser les guillemets simples, mais il faut échapper l'apostrophe !
console.log("JavaScript c'est facile"); // On peut aussi utiliser les guillemets doubles, plus facile dans ce cas particulier

// Affichez la phrase suivante avec les guillemets et les apostrophes :
// C'est Lao Tseu qui a dit : "L'échec est le fondement de la réussite"
console.log('C\'est Lao Tseu qui a dit : "L\'échec est le fondement de la réussite"'); // Il ne faut échapper que les guillemets simples
console.log("C'est Lao Tseu qui a dit : \"L'échec est le fondement de la réussite\""); // Il ne faut échapper que les guillemets double